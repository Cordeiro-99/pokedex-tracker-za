# Pokedextracker - Versão Pessoal

## Setup

1. `npm install`
2. `npm run dev`
3. abrir `http://localhost:5173`

## Como adicionar a Pokédex ZA
- Editar `src/data/pokedex.json` com a lista completa.
- Colocar sprites em `public/sprites/` e referenciá-los no campo `sprite`.
